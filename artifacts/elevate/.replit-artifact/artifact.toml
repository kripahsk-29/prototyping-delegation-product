kind = "web"
previewPath = "/"
title = "Elevate — Delegation Intelligence"
version = "1.0.0"
id = "artifacts/elevate"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23512

[services.development]
run = "pnpm --filter @workspace/elevate run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/elevate", "run", "build" ]
publicDir = "artifacts/elevate/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23512"
BASE_PATH = "/"
